export const metadata = {
  title: 'Custom Digital Solutions | Black Mirage',
  description: 'Transform your business with custom digital solutions tailored to your unique needs. From web applications to enterprise software, we build solutions that drive growth.',
};

export default function Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
